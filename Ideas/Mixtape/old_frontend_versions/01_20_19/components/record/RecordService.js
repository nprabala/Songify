app.service('recordService', function() {
	// Draws the main staff for melody input
	this.init = function (x, y) {
		// Loads preset amount of measures, buttons, functionality for staff
	}

	// Given an object, draw on canvas
	this.draw = function (x, y) {

	}

	// Checks notes are correctly displayed
	this.refresh = function () {

	}
})