'use strict';

describe('mixTapeApp.record module', function() {

  beforeEach(module('mixTapeApp.record'));

  describe('record controller', function(){

    it('should ....', inject(function($controller) {
      //spec body
      var recordController = $controller('RecordController');
      expect(recordController).toBeDefined();
    }));

  });
});